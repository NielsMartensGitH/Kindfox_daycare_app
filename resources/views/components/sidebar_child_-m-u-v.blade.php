<div class="col-sm-12">
    <h4 class="my-2">my children</h4>
    <?php /*
    <!--
    @foreach ($children as $child)

    <a class="kiddo noselect" (click)="addChildId(child.id)" routerLink="childdiaries"><i class="fas fa-baby border rounded-circle p-2"></i><span class="mx-2"> {{child->firstname}} </span></a>
        @if({{child->checked_in}} === 1)
            <span class="yesdot"></span>
        @elseif({{child->checked_in}} === 0)
            <span class="nodot"></span>
        @endif
    @endforeach
    -->
    */?>
    </div>
</div>